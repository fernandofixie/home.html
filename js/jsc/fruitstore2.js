function runActivity () {

while (true) {
  let choice_name = prompt("What would you like to buy? (0) Stop");
  let fruit_found = false; //up to you if you use this

  for (/* have this for loop go through the fruit names */){
      if (fruit_names[i] == choice_name){
          //ask the user for how many fruits they want to buy
          //calculate the total based on the fruit's actual price x amount the user wants to buy. use the i variable.
      }
  }
}
}